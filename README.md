Usage Instructions
Install dependencies:

bash
Download
Copy code
sudo apt-get install aircrack-ng airodump-ng
Run the tool:

bash
Download
Copy code
./wifi_hacker.py --interface wlan0 --wordlist /path/to/wordlist.txt
Use the interactive menu to:

Scan for networks
Target specific networks
Capture handshakes
Crack passwords
View statistics
Export logs
